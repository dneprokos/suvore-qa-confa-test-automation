# order-webhook-relay

relay for order webhooks

## setup

npm install, then copy .env.example to .env

IMPORTANT: run `npm run migrate` before the first start — the server refuses to boot against an unmigrated database.

Ops runbook: https://wiki.example.internal/relay-runbook
