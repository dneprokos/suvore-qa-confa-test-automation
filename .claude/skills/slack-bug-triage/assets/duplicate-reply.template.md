Looks like this one is already tracked as <DUPLICATE_KEY> — <DUPLICATE_SUMMARY> (<DUPLICATE_STATUS>).

<DUPLICATE_URL>

Matched on: <JUSTIFICATION>

If that is not the same problem, say so here and it will be filed separately.

_(automated triage)_
